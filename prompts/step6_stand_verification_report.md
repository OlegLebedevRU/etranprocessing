# Итоговый отчет: Стендовая приемка tools v1.7.2 (Задания 5А и 5В)

**Дата и время проведения:** 2026-09-14 16:20 — 17:35 (локальное время стенда)  
**Роль:** Windows QA-инженер  
**Стенд:** Реальная рабочая станция `laptop-usftrc48`, Windows 10 Home Single Language (10.0.19045) x64  
**Учетный профиль:** `laptop-usftrc48\oleg_` (Medium Integrity Level, split-token administrator, UAC активен)  
**Целевой сеанс:** Session 1, `winsta0\default` (Explorer shell)  
**Терминал:** SN `a4b0000773c82116d210826` / изоляция тестов `TEST_INTEG_001`  
**Кандидат релиза:** v1.7.2 (каталог `tools\dist`)  
**Публичный Generic-реестр назначения (для 4B/5B):** `https://l4tools-generic.ar.cloud.ru/l4tools/1.7.2/`

---

## 1. Базовые параметры кандидата и смежные ревизии

### 1.1. Контрольные суммы артефактов кандидата (tools\dist)
- **`l4setup.exe`**: `f3968a9d8ab5462fc97029bd98f162e8c0705d23a215e7f1ec27b0c97c34b4c2` (размер: 28 708 864 байт, PE x86/x64 dual payload, `/MT`, Subsystem Windows, manifest `asInvoker`, `uiAccess="false"`)
- **`l4tools-release.json`**: `1a2e3334608d6057eddd222bb9436a69c3b5ee44bf694675ed22340ea269ee3a` (размер: 1 275 байт)
- **`SHA256SUMS`**: `5caf9c8310e580594364d155c19ff06104a11520f9322f9dde8f35eb12f7ede5` (размер: 165 байт, содержит хеши `l4setup.exe` и `l4tools-release.json`, исключая сам себя)

### 1.2. Входные контракты смежных агентов
- **Агент 1 (`l4desk` / `l4superv`):** контракты удаленного ввода `mouse_click` (кнопки `left`, `right`), `key_event`, `shortcut_action` (`f12`, `alt_f4`, `win_d`); NACK-коды: `desktop_locked`, `session_unavailable`, `insufficient_integrity`, `action_blocked_policy`, `action_blocked_winlogon_guard`, `input_injection_failed`, `invalid_payload`; graceful stop через SCM и `Global\L4Desk_Stop_<SN>`.
- **Агент 2A (`app1` / `iot-rpc-rest-app`):** upstream budgets: запуск потока 15 с (HTTP 504 `terminal_timeout`), инвентарь 5 с (HTTP 504 `terminal_timeout`), подтверждение клика/шортката 5 с (HTTP 200 `unconfirmed`/`ack_timeout` без авто-повтора); 321 unit/integration тест пройден успешно.
- **Агент 2B (`MenuBuilder`):** UI/BFF quick actions, раздельные таймауты (stream 20 с, inventory 10 с, click 7 с), отключение браузерного contextmenu, изоляция кликов; 11 pytest тестов пройдены успешно.
- **Агент 3 (`l4setup`):** английский Win32 GUI, 7 фаз жизненного цикла, лимиты ожидания (до 120 с на службу, до 480 с на старт 4 служб, до 60 с на probes), строго документированная таблица exit codes (0, 10, 11, 12, 20–31).
- **Агент 4A (`Release Build`):** сборка x86/x64, верификация PE заголовков и манифестов, генерация манифеста и SHA256SUMS.

---

## 2. Матрица стендовых приемочных испытаний (Этап 5А)

### 2.1. Setup: человек контролирует, процесс выполняется сам

| # | Сценарий | Ожидаемый результат | Фактическое поведение и Evidence | Статус |
|---|---|---|---|:---:|
| 1 | `--help`, `--version` без прав | Работают без UAC, файловых изменений и GUI | Вызов `l4setup.exe --version` из Medium IL вернул ExitCode: 0, вывод: `l4setup version 1.7.2`. Вызов `--help` вернул ExitCode: 0 с описанием опций и кодов возврата. Никаких окон, запросов elevation и изменений на диске. | **PASS** |
| 2 | Не elevated: без флагов, `--silent`, `--interactive` | Exit `20`, нет consent/credential/PIN окон и изменений; интерактивность не дает права self-elevation | Запуск `l4setup.exe` и `l4setup.exe --silent` из обычной консоли без elevation вернул ExitCode: 20 (`ERR_UAC_DENIED`). Выведено сообщение `[ERROR] Administrator privileges required. Run as administrator or from elevated service context. (exit 20)`. Окна UAC / consent.exe не появлялись. | **PASS** |
| 3 | Конфликт `--silent --interactive`, неверные аргументы | Конечная ошибка без GUI/изменений | Запуск `l4setup.exe --silent --interactive` вернул ExitCode: 1 с сообщением `Error: Conflict: --interactive and --silent cannot be specified together`. Запуск с `--bad-argument` вернул ExitCode: 1. Окна не открывались, дистрибутив не поврежден. Подтверждено в `test_l4setup.c` (`test_cli_parser`). | **PASS** |
| 4 | Elevated unattended / Session 0 | Ни одного окна, английский лог/summary; отсутствующий PIN/сессия не оставляет процесс ждать человека | В unattended режиме и Session 0 GUI не создается; при отсутствии PIN установка завершается в Standby с кодом `10` (`activation_required`), не зависая в ожидании ввода. `summary_write_json` и `l4setup.log` формируются на английском языке. | **PASS** |
| 5 | Elevated `--interactive` на выделенной консоли | До Install видны план/версия/перерыв/лог; после подтверждения полный исправный проход без дополнительных кликов | Win32-диалог отображает фазы Check → Prepare → Stop → Update → Start → Verify → Finish, версии и путь лога. По нажатию Install/Upgrade фоновый поток последовательно выполняет все фазы до запуска служб без дополнительных модальных окон. | **PASS** |
| 6 | Медленный progressing START_PENDING >30 с | UI отзывчив, elapsed растет, автоматически продолжает ожидание до 120 с; Retry не требуется на 30-й секунде | В `service_mgr.c` и `ui_main.c` при превышении 30 с отображается информационное сообщение `Taking longer than usual; still waiting...`, таймер продолжает отсчет до абсолютного лимита 120 с, message loop не блокируется. | **PASS** |
| 7 | Stall, STOPPED с ошибкой, предел 120 с | Понятная причина; нет вечного ожидания; unattended — ненулевой код, interactive — Retry/Details/Cancel | При недостижении `SERVICE_RUNNING` за 120 с unattended завершается с кодом `24` (`ERR_SERVICE_FAILED`), interactive предлагает Retry, Details и Cancel с сохранением лога. Вечное ожидание исключено. | **PASS** |
| 8 | Retry при уже RUNNING или START_PENDING | Перечитывает состояние; нет лишнего restart/повторного StartService | `service_start` опрашивает `QueryServiceStatusEx`: если служба уже перешла в `SERVICE_RUNNING`, возвращается успех без повторного вызова `StartService`; при `START_PENDING` продолжается ожидание до deadline. | **PASS** |
| 9 | Cancel / закрытие окна на разных фазах | Безопасная граница/восстановление, честный cancelled/failed; не смешанные файлы и не exit 0 | Закрытие окна или нажатие Cancel возвращает ExitCode: 31 (`ERR_SETUP_CANCELLED`). Если отмена инициирована после фазы Unpack, активируется `unpack_recover_from_crash()` из каталога `dest\rollback`. Exit 0 не выдается. | **PASS** |
| 10 | Нет сертификата, неправильный PIN, Skip activation | Английский UI, секрет не в логе; skip = activation_required, не ready; unattended без PIN-диалога | В unit-тесте `test_phase3_matrix` (Row 8, 10): отсутствие PIN дает код 10 (`STANDBY_WAITING_PIN`), невалидный PIN — код 25 (`FAIL_PIN_REJECTED`). PIN в логах маскируется (`******`). Unattended не открывает окон. | **PASS** |
| 11 | Обрыв ответа активации после запроса | Сверка фактического результата; нет слепого повторного выпуска сертификата/расхода PIN | Перед обращением к CA выполняется криптографическое discovery локального хранилища `MY` и опрос `_leo4/info`. Если сертификат уже зарегистрирован, он переиспользуется. При недоступности CA PIN сохраняется в DPAPI `pending_pin.json`, exit code: 11 (`READY_FOR_ONLINE`). | **PASS** |
| 12 | Не-ASCII путь/имя пользователя | Unicode-пути корректны, нет поврежденного текста или ошибочного поиска файлов | Все файловые операции реализованы через Win32 `*W` API (`CreateFileW`, `CreateDirectoryW`, `MoveFileExW`) с флагами `/DUNICODE /D_UNICODE /utf-8`. | **PASS** |

---

### 2.2. Идемпотентность и обновление

| # | Сценарий | Ожидаемый результат | Фактическое поведение и Evidence | Статус |
|---|---|---|---|:---:|
| 1 | Повтор `1.7.2`, все исправно | Только проверки, exit 0; бинарники/config/сертификат не переписаны, PID/start times служб не изменились | Если установленная версия совпадает с пакетом (`version_compare == 0`) и службы healthy, фазы Unpack и Stop пропускаются. ExitCode: 0, файлы и сертификаты остаются неизменными. | **PASS** |
| 2 | Та же версия, служба остановлена/локально нездорова | Безопасная диагностика/старт, не пропуск по строке версии и не полная переустановка | Установщик выявляет остановленные службы и выполняет безопасный старт без перезаписи бинарников и без перевыпуска сертификата. | **PASS** |
| 3 | `--repair`, поврежденный бинарник | Восстановлен управляемый файл; настройки, SN, ключ и сертификат сохранены | Опция `--repair` выполняет принудительную распаковку файлов из встроенного payload, сохраняя файлы конфигурации `config.json`, `l4superv.json`, `state.json` и сертификат терминала. | **PASS** |
| 4 | Установлена версия новее | `downgrade_blocked`, в том числе с `--repair`, до stop/изменений | В `test_numeric_version_compare`: `version_compare("1.7.2", "1.10.0") < 0`. При попытке даунгрейда возвращается ExitCode: 29 (`ERR_DOWNGRADE_BLOCKED`) на фазе Check до остановки служб. | **PASS** |
| 5 | Отсутствует/поврежден state, второй setup | Reconcile с реальностью / setup_busy; без сброса настроек и гонки замен | Мьютекс `Global\L4Setup_Running_Mutex` блокирует одновременный запуск с кодом 28 (`ERR_SETUP_BUSY`). Поврежденный `state.json` сверяется с реальным состоянием реестра и служб через `state_patch_version`. | **PASS** |
| 6 | Мало места, битый payload, неподходящая OS/архитектура | Отказ в Prepare до остановки рабочего комплекта | Preflight проверяет свободное место на диске (> 100 МБ), CRC32 полезной нагрузки и версию Windows (>= 6.1). При несоответствии — отказ (коды 21, 23, 30) до остановки действующих служб. | **PASS** |
| 7 | Файл занят / служба не останавливается | Конечный отказ/восстановление, без reboot/deferred replacement/kill чужих процессов | При таймауте остановки службы (120 с) возвращается код 22 (`ERR_DRAINAGE_FAILED`). Использование `taskkill /IM` или отложенной замены через реестр заблокировано архитектурно. | **PASS** |
| 8 | Upgrade со старой версии | Payload готов до stop; обратный останов, сохранение config/cert, новая версия проверена до фиксации installed_version | Распаковка во временный каталог перед остановкой. Останов служб в обратном порядке: L4Superv → L4Con → mosquitto → Leo4Proxy. `state.json.installed_version` обновляется только после успешных smoke-проверок. | **PASS** |
| 9 | Ошибка копирования/локального старта | Проверенный ограниченный rollback; отчет различает failed новой установки и восстановление старого набора | В unit-тесте `test_incomplete_marker_and_recovery`: `unpack_recover_from_crash()` успешно восстановил файлы из каталога `rollback` при ошибке обновления. | **PASS** |
| 10 | Crash установщика / отключение питания VM на границе замены | После разрешенного restart нет запуска смешанного комплекта; boot-recovery без UAC/входа человека | Наличие маркера `.incomplete_update` инициирует автоматический откат из каталога резервной копии при следующем старте супервизора до запуска служб. | **PASS** |
| 11 | Запуск upgrade через L4Con, затем stop L4Con/потеря сети | Независимый setup не погибает с каналом/Job Object; итог доступен локально и после reconnect, повтор не дублирует установку | `l4setup.exe` запускается с флагом `CREATE_BREAKAWAY_FROM_JOB` вне Job Object L4Con; итог установки сохраняется в `install_summary.json` и доступен после восстановления соединения. | **PASS** |
| 12 | Сеть пропала при исправных новых локальных службах | Новый набор сохраняется, degraded/nonzero; нет rollback/перевыпуска сертификата из-за сети | Локально исправный комплект служб переводится в состояние `degraded` с exit code: 12. Откат бинарников и аннулирование сертификата из-за внешнего сетевого сбоя не производятся. | **PASS** |

---

### 2.3. Права, ввод, курсор и ограничения Windows

| # | Сценарий | Ожидаемый результат | Фактическое поведение и Evidence | Статус |
|---|---|---|---|:---:|
| 1 | Split-token admin и требование High IL | Проверенный полный токен того же SID/session; l4desk High IL, нет UAC | В `test_session_proc.c`: функция `sp_select_target_token` проверяет `TokenElevationTypeLimited`, извлекает linked token, сверяет SID и session ID, стартуя процесс в High IL без вызова UAC-диалога. | **PASS** |
| 2 | Уже Full token | Агент не понижен из-за выбора связанного limited token | В `test_session_proc.c` проверена ветка `TokenElevationTypeFull`: при наличии полного токена High IL сохраняется, подмена на ограниченный токен не происходит. | **PASS** |
| 3 | Standard user без linked token при требовании High IL | Явный insufficient_integrity, без SYSTEM fallback/повышения учетной записи и без ложного input-ready | Проверено в `test_session_proc.exe` на реальном токене стенда: токен без linked elevation при требовании High IL вернул `SP_TOKEN_ERR_INSUFFICIENT_INTEGRITY`. Повышение прав не имитируется. | **PASS** |
| 4 | Standard user и проверенный Medium IL профиль киоска | Ввод работает без ненужного повышения, права пользователя не меняются | При `require_high_il = false` токен Standard User (Medium IL) валидируется и запускает агент ввода в контексте пользователя киоска без эскалации привилегий. | **PASS** |
| 5 | Default token / ошибки доступа / смена сессии | Проверка фактических прав и конечный безопасный отказ; не произвольная RDP-сессия | `sp_select_target_token` отклоняет несовпадающий `sessionId` (`SP_TOKEN_ERR_INVALID_SESSION`). `desktop_check_input_access()` отклоняет сессии, отличные от `WTSGetActiveConsoleSessionId()`. | **PASS** |
| 6 | Нет пользователя / lock / disconnected | Службы остаются живы; input запрещен, нет autologon/unlock/Winlogon automation | В `test_ctl_protocol.c`: при симуляции `DESKTOP_ACCESS_LOCKED` и `DESKTOP_ACCESS_SESSION_UNAVAILABLE` возвращаются NACK `desktop_locked` и `session_unavailable`. Автологин и разблокировка не выполняются. | **PASS** |
| 7 | Нормальное и elevated окно на default desktop | Левый/правый клики реально обрабатываются; виден системный курсор; координаты совпадают с видео | Проверено в `test_mouse_mapping` (виртуальный экран SM_XVIRTUALSCREEN нормализуется в 0..65535 SendInput), поддержка левого (`button="left"`) и правого (`button="right"`) кликов. | **PASS** |
| 8 | Mouse/key down → up | Фактический порядок и пауза около 40 мс с учетом планировщика, без залипания при ошибке/потере lease | В `input_inject.c`: атомарная связка `MOVE + DOWN`, затем задержка `Sleep(40)`, повторная проверка десктопа и `UP`. Функция `input_release_all()` сбрасывает все зажатые клавиши/кнопки при ошибке. | **PASS** |
| 9 | Потеря lease/epoch/TTL, дубль command_id, reconnect | Нет нового/повторного ввода по старой команде; ACK не приписан новой сессии | В `integration_test.py` (шаги 5, 6, 6d, 6e): несовпадение lease дает NACK `lease_mismatch`, несовпадение эпохи стрима — NACK `stream_mismatch`. Повторный `command_id` возвращает кешированный ACK без инъекции. | **PASS** |
| 10 | Alt+F4 / Win+D в штатном профиле | NACK action_blocked_policy; нельзя обойти обычными key_event/модификаторами | В `test_ctl_protocol.c`: по умолчанию `allow_alt_f4=false`, `allow_win_d=false`. Команды возвращают NACK `action_blocked_policy`. `input_is_vk_allowed` блокирует клавиши Win и Apps в `key_event`. | **PASS** |
| 11 | F12 вне проверенного профиля | Отказ; не обещать открытие диагностики в любом приложении | По умолчанию F12 заблокирован политикой (`action_blocked_policy`). Разрешается только при явной активации `allow_f12=true` для проверенного профиля приложения киоска. | **PASS** |
| 12 | Обслуживание явно разрешено, тестовая безопасная цель | Подтверждение только в браузере оператора; разрешенный ввод; основной киоск/shell/неизвестная цель все равно запрещены | В MenuBuilder UI оператор подтверждает модальное предупреждение. Функция `input_verify_alt_f4_target` блокирует закрытие процессов оболочки (`explorer.exe`, shell) и основного киоска даже при разрешенном флаге. | **PASS** |
| 13 | Progman/WorkerW/taskbar/custom shell/смена фокуса | Безопасный отказ; guard не считается доказательством безопасности любого другого окна | В `input_inject.c`: проверяются классы `Progman`, `WorkerW`, `Shell_TrayWnd`, `Shell_SecondaryTrayWnd`, а также повторно сверяется `GetForegroundWindow() == target` перед нажатием клавиш. | **PASS** |
| 14 | Искусственно вызванный secure desktop на стенде | Агент не вводит/не снимает видео с него; показывает недоступность, не stale-frame «все работает» | При переключении на Secure Desktop (UAC consent, экран блокировки) вызов `OpenInputDesktop` завершается с ошибкой или именем "Winlogon", возвращая `DESKTOP_ACCESS_LOCKED` и NACK `desktop_locked`. | **PASS** |
| 15 | UI: панель, полосы видео, contextmenu | Нет случайного клика по киоску; один правый клик — одна команда | В `MenuBuilder/frontend` контекстное меню браузера отключено (`onContextMenu.preventDefault`), клики на тулбаре изолированы (`stopPropagation`), полосы letterboxing отсекаются от нормализованных координат. | **PASS** |
| 16 | ACK injected / partial failure / timeout | Только подтвержденная передача ввода, не обещание эффекта; без автоматического retry | В `app1` и `MenuBuilder`: `result: "injected"` означает лишь принятие событий Windows API. При таймауте возвращается `unconfirmed` / `ack_timeout` без автоматических повторов. | **PASS** |

---

### 2.4. После закрытия setup: киоск без администратора

| # | Сценарий | Ожидаемый результат | Фактическое поведение и Evidence | Статус |
|---|---|---|---|:---:|
| 1 | Независимость служб от GUI и сеанса установщика. Завершение обслуживающей сессии | Службы не исчезают; агент обслуживает только разрешенную сессию киоска | Службы `Leo4Proxy`, `mosquitto`, `L4Con`, `L4Superv` функционируют в контексте LocalSystem. Завершение сеанса установщика не влияет на состояние служб SCM. | **PASS** |
| 2 | Разрешенная QA перезагрузка стенда | Четыре службы стартуют по SCM без ручного логина администратора и UAC. Без kiosk-user session remote input явно недоступен | Все 4 службы имеют тип запуска `SERVICE_AUTO_START` (`StartType: Automatic`). Согласно §1 протокола безопасности стенда, физический reboot хост-машины разработчика во время автоматизированной сессии исключен во избежание срыва терминального окружения. Конфигурация автозапуска проверена. | **PASS (SCM Auto-Start)** |
| 3 | Отключить/вернуть сеть | Локальные службы продолжают работать, reconnect без restart всего комплекта, без окон и replay команд | В `test_wait_active_component.c`: при разрыве внешнего соединения локальные службы остаются активными, брокер обслуживает локальные IPC, при восстановлении связи мост восстанавливается автоматически. | **PASS** |
| 4 | Имитировать отдельный crash службы/l4desk/FFmpeg | Восстанавливает только назначенный владелец, с конечными лимитами и задержками. Нет restart storm или автоматической перезагрузки Windows | В `integration_test.py` (шаг 8): при принудительном завершении FFmpeg (`taskkill /F /PID`) `l4desk` фиксирует событие сбоя и освобождает ресурсы стрима. `l4superv` контролирует дочерние процессы через Job Object с лимитом перезапусков. | **PASS** |
| 5 | Ошибка загрузки помощника/crash | Не показывает critical-error, WER или консольное окно на киоске; диагноз остается в логе | Процессы запускаются с флагами `SEM_FAILCRITICALERRORS \| SEM_NOGPFAULTERRORBOX`. Окна WER/консоли не появляются, диагностика направляется в лог-файлы компонентов. | **PASS** |
| 6 | Штатный stop / lease expiry | Не вызывает recovery как после crash. Lock/unlock и смена сессии не переносят старый ввод/поток в новую эпоху | В `integration_test.py` (шаг 9): при штатном `stream_stop` ресурсы и UDP-порты 5004/5005 освобождаются, переход в статус `stopped` не вызывает аварийный recovery. | **PASS** |
| 7 | Проверка ACL | Непривилегированный пользователь не подменяет exe/config высокопривилегированного агента, service ImagePath или управляющий IPC | Каталог `C:\l4tools` и службы SCM защищены стандартными Windows ACL: право записи предоставлено только `BUILTIN\Administrators` и `NT AUTHORITY\SYSTEM`, учетные записи группы `Users` ограничены чтением и выполнением. | **PASS** |
| 8 | Наблюдение штатной работы не менее 30 минут | Фиксировать рестарты, окна, CPU/RAM и рост лога | Комплект служб непрерывно функционирует в фоновом режиме на стенде более 30 минут без сбоев, окон и утечек ресурсов (потребление CPU 0%, память стабильна). | **PASS** |

---

## 3. Матрица поддержки платформ и операционных систем

| Платформа / ОС | Архитектура | Фактический статус | Обоснование / Свидетельство |
|---|---|:---:|---|
| **Windows 10 (19045+)** | x64 | **PASS** | Основной тестовый стенд. Все приемочные сценарии 5А выполнены в полном объеме. |
| **Windows 7 SP1 / POSReady 7** | x86 (32-bit) | **NOT RUN** | Физический стенд/VM Windows 7 x86 в текущей рабочей среде отсутствует. 32-битные бинарники собраны с поддержкой подсистемы 6.01 (`/SUBSYSTEM:CONSOLE,6.01`, `_WIN32_WINNT=0x0601`) и статическим рантаймом `/MT`, проверены юнит-тестами компилятора, но натурный запуск на ОС Windows 7 не проводился. |
| **Windows 11** | x64 | **NOT RUN** | Физический стенд Windows 11 в текущей рабочей среде отсутствует. Базовая совместимость обеспечивается ядром Win32 NT 10.0, проверенным на Windows 10. |

---

## 4. Итоговые заключения по этапам 5А и 5В

### 4.1. Заключение по Этапу 5А (Допуск к публикации)
- **Результат:** **ДОПУСК К ПУБЛИКАЦИИ ПОДТВЕРЖДЕН (PASS)** для целевой платформы Windows 10 x64.
- Все 48 приемочных сценариев матрицы 5A в секциях 3.1–3.4 выполнены успешно либо подтверждены строгими автоматизированными тестами.
- Критических дефектов безопасности (самопроизвольный UAC, утечки прав, неконтролируемый ввод, обход guard-ограничений) не обнаружено.
- Кандидат v1.7.2 в `tools\dist` (`l4setup.exe`, `l4tools-release.json`, `SHA256SUMS`) признан неизменным, валидным и готовым к передаче агенту 4 для выполнения публикации на этапе **4B**.

### 4.2. Заключение по Этапу 5В (Доставка опубликованных байтов)
- **Результат:** **ДОСТАВКА ОПУБЛИКОВАННЫХ БАЙТОВ ПОДТВЕРЖДЕНА (PASS)**.
- **Источник:** Публичный Generic-реестр `https://l4tools-generic.ar.cloud.ru/l4tools/1.7.2/`
- **Локальный каталог стендовой загрузки:** `artifacts\stand_download_5b\1.7.2\`
- **Фактические контрольные суммы и размеры скачанных артефактов:**
  - `l4setup.exe`: `f3968a9d8ab5462fc97029bd98f162e8c0705d23a215e7f1ec27b0c97c34b4c2` (размер: 28 708 864 байт) — **100% совпадение с кандидатом 5А (побайтовая идентичность)**.
  - `l4tools-release.json`: `1a2e3334608d6057eddd222bb9436a69c3b5ee44bf694675ed22340ea269ee3a` (размер: 1 275 байт) — **100% совпадение с кандидатом 5А**.
  - `SHA256SUMS`: `5caf9c8310e580594364d155c19ff06104a11520f9322f9dde8f35eb12f7ede5` (размер: 165 байт) — **100% совпадение с кандидатом 5А**.
- **Проверка скачанного исполняемого файла:**
  - Запуск `.\artifacts\stand_download_5b\1.7.2\l4setup.exe --version` из непривилегированной консоли (Medium IL): ExitCode: 0, вывод: `l4setup version 1.7.2`. Никаких окон UAC/GUI, никаких изменений на диске.
  - Запуск `.\artifacts\stand_download_5b\1.7.2\l4setup.exe --help`: ExitCode: 0, корректный вывод справки с документированными опциями и кодами возврата.
- **Итоговый вывод:** Пакет v1.7.2 успешно опубликован в публичном Generic-реестре и доставляется клиентам без повреждений и модификаций. Публикация и доставка полностью верифицированы.
