# Бета-CI/CD — итоговая передача

- Запрос: builder 176.108.247.249, production 87.242.100.34; без SaaS CI-биллинга,
  версии кода → registry → версии runtime. Установка и отдельный ключ согласованы.
- Source of truth: `.github/ci/`, `deploy/beta/`, Dockerfile и lock-файлы в Git.
  Серверные кодовые файлы поставлены из commit штатным установщиком.
- Launcher `52e93a9`, worker и MenuBuilder release `cd5658c`; digest
  `sha256:af7e054da54eafd71c2ca1e1931ef136a68ccbed3ce91c6718dad73299cedb03`.
- Образы всех пяти компонентов собраны/опубликованы; реально обновлён только
  MenuBuilder backend. Девять соседних контейнеров остались прежними.
- Проверено: 28 CI-тестов; 106 ProcessingBackend и 278 MenuBuilder; 13 frontend;
  ingress make test; production frontend build; Ruff/Pyright; SSH с pinned host key;
  registry push/pull; revision/runtime health; повторное использование digest;
  timer enabled/active и успешный запуск без лишнего деплоя.
- Исправления с воспроизведением: Vitest отсутствовал в чистой установке frontend;
  sparse checkout пропускал ProcessingBackend proxy fixture. Тесты не исключались.
- Одноразовый старт наблюдения не меняет реальные releases и не пропускает
  application changes; GitHub workflow отключён, двойной деплой исключён.
- Rollback: остановить timer; применить previous image override только выбранного
  сервиса; проверить API и согласовать checkpoint/override. БД автоматически не
  понижается. Runtime routes/Janus/.env/сертификаты не поставляются этим flow.
- Ограничения: четыре остальных production-деплоя не выполнялись; миграции должны
  быть обратно совместимы; дисковая retention рабочей копии/uv cache пока ручная;
  исходные GitHub billing-настройки не менялись.
- Полные команды и evidence: [бета-регламент](../../../docs/ops_run-beta-ci-cd.md).