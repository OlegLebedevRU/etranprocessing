server {
    listen 8443 ssl;

    ssl_protocols TLSv1.2;
    ssl_ciphers 'ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384:AES128-GCM-SHA256:AES256-GCM-SHA384:ECDHE-RSA-AES128-SHA256:ECDHE-RSA-AES256-SHA384:ECDHE-RSA-AES128-SHA:ECDHE-RSA-AES256-SHA:AES128-SHA256:AES256-SHA256:AES128-SHA:AES256-SHA:@SECLEVEL=1';
    ssl_prefer_server_ciphers on;
    ssl_conf_command SignatureAlgorithms RSA+SHA256:RSA+SHA384:RSA+SHA512;
    ssl_conf_command ClientSignatureAlgorithms RSA+SHA256:RSA+SHA384:RSA+SHA512;

    ssl_certificate         /crt/server_certificate.pem;
    ssl_certificate_key     /crt/server_key.pem;
    ssl_client_certificate  /crt/ca_certificate.pem;
    ssl_verify_client       on;
    ssl_verify_depth        2;

    proxy_pass              ingress:9000;
    proxy_connect_timeout   5s;
    proxy_timeout           3600s;
    proxy_socket_keepalive  on;
}
